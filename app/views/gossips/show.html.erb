<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <section class="container">
      <div class="row mt-5 mb-3">
        <div class="col">
          <h1 class="text-center">Voici le gossip</h1>
          <br><br>
        </div>
      </div>
      <div class="card mb-3">
        <div class="card-body">
          <p class="card-text">
            <h2><%= @gossip.title %></h2><br>
            <h4><%= @gossip.content %></h4><br>
            <p><%=@gossip.user.first_name%> <%=@gossip.user.last_name%>, <%=@gossip.user.city.name%></p><br>
          </p>
          <p>Plus d'infos sur l'auteur ?</p>
          <a href="<%=gossip_user_path(@gossip.id, @gossip.user.id)%>"><button type="button" class="btn btn-primary">Show</button></a>
          <a href="<%= edit_gossip_path(@gossip.id) %>"><button type="button" class="btn btn-primary">Edit</button></a>
          <a href="<%= gossip_path(@gossip.id) %>" class="btn btn-danger" data-confirm="Are you sure ?" data-method="delete">Delete</a>
        </div>
      </div>
    </section>
  </body>
</html>
